#include <iostream>
using std::cout;
using std::endl;
int main()
{
    // ((12 / 3) * 4) + (5 * 15) + ((24 % 4) / 2) = 91
    cout << 12 / 3 * 4 + 5 * 15 + 24 % 4 / 2 << endl;   // 91
    return 0;
}
